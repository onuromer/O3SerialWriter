# O3SerialWriter
Lightweight Arduino logging library for consistent Serial output. Adds optional prefix, millis timestamp, log levels with filtering, and type-safe variadic logging (C# params style) so you can use this without repetitive Serial.prints. chains.
